# v1.3 — Sync Improvements

- Added realtime hinting via Supabase `postgres_changes` to trigger debounced syncs
- Foreground sync on activation, periodic background sync
- Stability and error handling improvements


